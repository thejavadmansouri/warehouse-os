# بکلاگ — وضعیت به‌روزشده (۱۴ اوت ۲۰۲۶)

> هر مورد با بررسی مستقیم کد تأیید شده است، نه صرفاً با ادعای گزارش قبلی.
> وضعیت کامیت: `e635ae1` = POS + امنیت (محلی، **هنوز push نشده**).
> کارهای D1 و D3 + تست‌هایشان هنوز کامیت نشده‌اند.

## وضعیت کامیت‌ها

| کامیت | محتوا | push |
|-------|-------|------|
| `e635ae1` | A1–A5، C3، D2 (CORS)، D10، D11، D12، D13 + مهاجرت xlsx | ❌ محلی |
| (کامیت نشده) | D1 (throttler)، D3 (user DTOs)، تست throttler، تست user.dto، اصلاح تست uploads | — |

---

## A) باگ‌های POS — اولویت بالا — `apps/web/src/app/admin/pos/`

- [x] **A1** مبلغ نهایی از ناحیه‌ی اسکرول خارج و به پایین ستون چسبید — فقط خلاصه اقلام اسکرول می‌شود
- [x] **A2** بعد از ثبت فاکتور، کش `["customer", id]` و `["customer-today-count", id]` بلافاصله invalidate می‌شود
- [x] **A3** دو کوئری «خریدهای امروز» → یک کوئری واحد با `pageSize: 5` (چیپ + پنل مشتری)
- [x] **A4** پنل مشتری شماره‌ی اصلی (`isPrimary`) را نشان می‌دهد — همراستا با چیپ
- [x] **A5** رنگ‌های غیرتوکن (`bg-blue-600`، `bg-amber-50` و…) → توکن‌های تم در کل پوشه POS
- [ ] **A6** ورودی «توضیح فاکتور» زیر مبلغ نهایی فضای زیادی می‌گیرد — کوچک/انتقال به دیالوگ تسویه

## B) بهبودهای POS — `apps/web/src/app/admin/pos/page.tsx`

- [ ] **B1** `errorLine` تکی → `errorLines: number[]` تا چند ردیفِ کم‌موجودی هم‌زمان قرمز شوند
- [ ] **B2** Undo برای حذف ردیف / پاک‌کردن سبد (تاریخچه‌ی reducer)
- [ ] **B3** انتخابگر تعداد با Shift+Enter (دیالوگ کوچک «تعداد»)
- [ ] **B4** تصویر کالا در ردیف‌های سبد (قطعات خودرو از تصویر بهره می‌برند)
- [ ] **B5** صدای/لرزش اسکن (Web Audio + `navigator.vibrate`)

**انجام نشود** (تأیید شده: از قبل درست/انجام‌شده است): دیبونس جستجو (۱۲۰ms + حداقل ۲ حرف)، باگ دزدیدن فوکوس، بازنویسی page.tsx به state machine / Zustand.

## C) UI عمومی

- [ ] **C1** سایدبار به‌هم‌ریخته — منتظر اسکرین‌شات/توضیح دقیق کاربر است (ادعا از کد اثبات‌پذیر نبود)
- [ ] **C2** بنر «سرور قطع شده» با هر خطای تکی ظاهر می‌شود — باید ۲ شکست پیاپی یا retry کوتاه لازم شود
- [x] **C3** حذف `userScalable:false` و `maximumScale:1` از viewport (WCAG 1.4.4)
- [ ] **C4** تجمیع داشبورد سمت کلاینت → endpoint/aggregate سمت دیتابیس (اولویت پایین)

## D) امنیت — `apps/api`

- [x] **D1** محدودیت نرخ لاگین — Throttler 5/min/IP فقط روی `POST /auth/login` + تست 429 ✅ (کامیت نشده)
- [x] **D2** CORS از `origin:true` → allowlist از `CORS_ORIGINS` (env) با پیش‌فرض امن LAN (لوپ‌بک + RFC1918 + Tauri) + تست
- [x] **D3** `body:any` و inline types حذف → `CreateUserDto`/`ChangeRoleDto`/`ChangePasswordDto` + ۷ تست ValidationPipe ✅ (کامیت نشده)
- [x] **D4 (verify)** `$queryRawUnsafe` در product-matcher — تأیید شد **امن**: فقط placeholder، بدون concat ورودی کاربر
- [x] **D5 (verify)** بکاپ‌ها — تأیید شد **امن**: restore فقط `ADMIN`، بقیه `ADMIN/MANAGER`
- [ ] **D6** اکسپورت اکسل از صفحه‌بندی عبور می‌کند (سقف ۱۰هزار ردیف) — DoS خفیف (اولویت پایین)
- [ ] **D7** اسکوپ انبار روی kardex/statement/reports — قبل از چند‌انباری حتماً
- [ ] **D8** Tauri `csp:null` → CSP محدود
- [ ] **D9** BASE_URL اندروید (IP داخلی هاردکد) → `local.properties` (gitignored)
- [x] **D10** سرو static `/storage` → فقط `storage/products`؛ عکس‌های انبار فقط از `GET /uploads/photo/:assetId` احرازشده
- [x] **D11** آپلودهای عکس از pipeline تاییدشده عبور می‌کنند: سقف ۵MB + MIME + sniff + sharp + تامب‌نیل (هر سه مسیر)
- [x] **D12** xlsx 0.18.5 (CVE-2023-30533، CVE-2024-22363) → **0.20.3** از CDN رسمی + تست دود خواندن/نوشتن
- [x] **D13** helmet اضافه شد + تست هدرها (nosniff، X-Frame-Options، HSTS، CSP، حذف X-Powered-By)

## ادعاهای FALSE گزارش قبلی

- **(a) «backup_before_seed.sql فقط در working tree است»** — ❌ ادعا **غلط بود**: فایل در تاریخچه‌ی گیت بود و روی گیتهاب لو رفته بود. وضعیت فعلی: **کاملاً پاک‌سازی شد** ✅ — هر دو شاخه بازنویسی و force-push شدند، فایل لوکال حذف شد، `*.dump`/`storage/` به `.gitignore` اضافه شد. باقی‌مانده: تأیید privacy ریپو (کار مالک).
- **(b) «AppExceptionFilter پیام خطا لو می‌دهد»** — ✅ تأیید شد FALSE: پیام عمومی برمی‌گرداند، stack فقط سمت سرور لاگ می‌شود. کاری لازم نیست.

## نیازمند ممیزی واقعی (هنوز انجام نشده)

- [ ] WebSocket — احراز هویت per-connection
- [ ] IDOR — بررسی endpoint به endpoint
- [ ] پوشش AuditLog

---

## آمار تست (آخرین اجرا)

- ✅ API: **۱۱ سوئیت / ۵۳ تست — همه پاس** + `npx tsc --noEmit` در هر دو اپ سبز
- ✅ تست‌های موقت امنیتی (CORS، helmet، xlsx، throttler) اجرا و حذف شدند
- ⚠️ `npm audit` در دسترس نیست (رجیستری پروژه endpoint امنیتی را پیاده نکرده)
