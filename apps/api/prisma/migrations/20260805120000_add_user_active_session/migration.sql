-- نشست فعالِ هر کاربر. افزایشی و nullable — ردیف‌های موجود دست نمی‌خورند.
-- توکن‌های صادرشده پیش از این تغییر شناسه‌ی نشست ندارند و بی‌اعتبار می‌شوند،
-- یعنی همه یک بار باید دوباره وارد شوند. این عمدی است.
ALTER TABLE "User" ADD COLUMN "activeSessionId" TEXT;
